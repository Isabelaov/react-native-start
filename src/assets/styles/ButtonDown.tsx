import { StyleSheet } from 'react-native';
import { baseColors } from '../colors/baseColors';

export const ButtonDown = StyleSheet.create({
  button: {
    backgroundColor: baseColors.primary,
    padding: 5,
    margin: 20,
    borderRadius: 10,
  },
});
